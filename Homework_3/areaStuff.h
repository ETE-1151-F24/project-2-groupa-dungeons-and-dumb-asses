#ifndef AREASTUFF_H       //if my [areaStuff.h] isnt already defined...
#define AREASTUFF_H       //then define [AREASTUFF_H] with the following code lines as seen below
void squareArea(int side);      //prelimi declaring a function called [squareArea] and expecting a [int] parameter named [side] stored at input
void rectangleArea(int n);        //prelimi declaring a function called [rectangleArea] and expecting a integer [int] variable [n]
void floaterToBinary(float f);  //prelimi declaring a function called [floaterToBinary] and expecting a float [float] variable [f]
void floaterToBinary(float f);  //prelimi declaring a function called [floaterToBinary] and expecting a float [float] variable [f]

//this is the statement to end the definition of the [conversionStuff.h]
#endif // CONVERSIONSTUFF_H